# apiVersion: eksctl.io/v1alpha5
# kind: ClusterConfig

# metadata:
#   name: expense
#   region: us-east-1

# managedNodeGroups:
#   - name: expense
#     instanceTypes: m5.large
#     desiredCapacity: 3
#     spot: true


# apiVersion: eksctl.io/v1alpha5
# kind: ClusterConfig

# metadata:
#   name: expense
#   region: us-east-1

# managedNodeGroups:
#   - name: expense
#     instanceTypes:
#       - m5.large
#     desiredCapacity: 3
#     spot: true

# Updated
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: expense
  region: us-east-1

managedNodeGroups:
  - name: expense
    spot: true
    instanceTypes:
      - m5.large
      - m5a.large
      - m4.large
      - c5.large
      - c5a.large
    desiredCapacity: 3
    minSize: 3
    maxSize: 5


